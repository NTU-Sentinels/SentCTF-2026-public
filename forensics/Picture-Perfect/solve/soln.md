## Solution
To solve this, first we need to check the file header to see what is the original file extension supposed to be.

Then change it to JPEG / PNG (PNG is the original file header)

Then by using any online tool or photoshop, manipulate the RGB Stream to see the flags.

